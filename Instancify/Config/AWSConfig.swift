import Foundation

enum AWSConfig {
    static let poolId = "YOUR_POOL_ID"  // From step 4
    static let clientId = "YOUR_CLIENT_ID" // From step 4
    static let region = "YOUR_REGION" // From your pool ID (e.g., "us-east-1")
} 